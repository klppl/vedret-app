package se.vedret.app.ui.theme

import androidx.compose.ui.graphics.Color

// Rosé Pine palette — https://rosepinetheme.com/palette/
// Mapped to the app's roles: Bg / Surface / Ink / Muted / Faint / Line / Accent / Rain.
object RosePine {
    val Bg = Color(0xFF191724)       // base
    val Surface = Color(0xFF1F1D2E)  // surface
    val Overlay = Color(0xFF26233A)  // overlay
    val Ink = Color(0xFFE0DEF4)      // text
    val Muted = Color(0xFF908CAA)    // subtle
    val Faint = Color(0xFF6E6A86)    // muted
    val Line = Color(0xFF403D52)     // highlight med
    val Accent = Color(0xFFF6C177)   // gold
    val Rain = Color(0xFF9CCFD8)     // foam
    val Love = Color(0xFFEB6F92)     // love
}

object RosePineDawn {
    val Bg = Color(0xFFFAF4ED)       // base
    val Surface = Color(0xFFFFFAF3)  // surface
    val Overlay = Color(0xFFF2E9E1)  // overlay
    val Ink = Color(0xFF575279)      // text
    val Muted = Color(0xFF797593)    // subtle
    val Faint = Color(0xFF9893A5)    // muted
    val Line = Color(0xFFDFDAD9)     // highlight med
    val Accent = Color(0xFFEA9D34)   // gold (dawn)
    val Rain = Color(0xFF56949F)     // foam (dawn)
    val Love = Color(0xFFB4637A)
}
